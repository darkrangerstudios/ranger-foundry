interface:
  display_name: "ranger-marshal"
  short_description: "Drive complex work from request to verified handoff"
  default_prompt: "Use $ranger-marshal to carry this software task through the right specialist stations and return verified evidence."
policy:
  allow_implicit_invocation: true
