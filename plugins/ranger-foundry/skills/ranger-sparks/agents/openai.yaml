interface:
  display_name: "ranger-sparks"
  short_description: "Build a small, testable proof of the idea"
  default_prompt: "Use $ranger-sparks to define and build the smallest testable prototype for this idea."
policy:
  allow_implicit_invocation: true
