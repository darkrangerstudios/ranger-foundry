interface:
  display_name: "Ranger Slice Plan"
  short_description: "Plan work as verifiable vertical slices"
  default_prompt: "Use $ranger-slice-plan to turn this objective into small, dependency-aware implementation slices."
policy:
  allow_implicit_invocation: true
