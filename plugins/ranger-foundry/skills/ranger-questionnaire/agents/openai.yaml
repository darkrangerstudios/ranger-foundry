interface:
  display_name: "Ranger Questionnaire"
  short_description: "Turn ambiguity into a decision-ready brief"
  default_prompt: "Use $ranger-questionnaire to clarify this request and produce a decision-ready brief."
policy:
  allow_implicit_invocation: true
