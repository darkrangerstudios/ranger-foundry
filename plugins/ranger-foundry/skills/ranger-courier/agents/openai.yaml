interface:
  display_name: "ranger-courier"
  short_description: "Create durable, evidence-backed task handoffs"
  default_prompt: "Use $ranger-courier to prepare a concise, resumable handoff for this task."
policy:
  allow_implicit_invocation: true
