interface:
  display_name: "ranger-phantom"
  short_description: "Bezier motion and precise browser interaction"
  default_prompt: "Use $ranger-phantom to diagnose this browser interaction and test a bounded, reproducible fix."
policy:
  allow_implicit_invocation: true
