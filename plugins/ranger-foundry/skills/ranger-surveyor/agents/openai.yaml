interface:
  display_name: "ranger-surveyor"
  short_description: "Resolve definitions, identity and retrieval meaning"
  default_prompt: "Use $ranger-surveyor to resolve the definitions and entity ambiguity in this evidence before using it."
policy:
  allow_implicit_invocation: true
