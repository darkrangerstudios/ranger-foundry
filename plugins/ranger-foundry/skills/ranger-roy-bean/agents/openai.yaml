interface:
  display_name: "ranger-roy-bean"
  short_description: "Write safe, scoped repository agent guidance"
  default_prompt: "Use $ranger-roy-bean to create or refine the agent guidance for this repository."
policy:
  allow_implicit_invocation: true
