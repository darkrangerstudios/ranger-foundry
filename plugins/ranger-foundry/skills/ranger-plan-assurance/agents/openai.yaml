interface:
  display_name: "Deadeye · Plan Assurance"
  short_description: "Stress-test plans before implementation begins"
  default_prompt: "Use $ranger-plan-assurance to review this plan for concrete gaps, unsafe sequencing, and missing gates."
policy:
  allow_implicit_invocation: true
