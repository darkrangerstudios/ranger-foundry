interface:
  display_name: "Ranger Handoff"
  short_description: "Create durable, evidence-backed task handoffs"
  default_prompt: "Use $ranger-handoff to prepare a concise, resumable handoff for this task."
policy:
  allow_implicit_invocation: true
